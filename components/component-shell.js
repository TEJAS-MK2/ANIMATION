const tabs=document.querySelectorAll('[data-code-tab]');const blocks=document.querySelectorAll('[data-code]');tabs.forEach(t=>t.addEventListener('click',()=>{tabs.forEach(x=>x.classList.remove('active'));blocks.forEach(x=>x.hidden=true);t.classList.add('active');const b=document.querySelector(`[data-code="${t.dataset.codeTab}"]`);if(b)b.hidden=false;}));document.querySelectorAll('[data-copy]').forEach(btn=>btn.addEventListener('click',async()=>{const b=document.querySelector(`[data-code="${btn.dataset.copy}"] code`);if(!b)return;try{await navigator.clipboard.writeText(b.textContent);const old=btn.textContent;btn.textContent='Copied';setTimeout(()=>btn.textContent=old,1200)}catch{btn.textContent='Copy failed'}}));