# Drawer

A dialog-based side panel for menus and contextual content.

- Uses the native `<dialog>` element and `showModal()`.
- Provides an explicit close control.
- Supports Escape through native dialog behavior.
- Respects `prefers-reduced-motion` by disabling the entrance animation.
- Keep essential content available without relying on animation.

Source: `components/drawer.html`.
