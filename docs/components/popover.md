# Popover

Accessible floating disclosure content opened from a button.

- Trigger exposes `aria-expanded` and `aria-controls`.
- Escape closes the popover.
- Clicking outside closes it.
- Keep the trigger and contained controls keyboard accessible.
- Avoid using it for essential content that should always be visible.

Source: `components/popover.html`.
