# Breadcrumbs

Semantic hierarchical navigation showing the user's current location.

- Use a labeled `<nav>` landmark and ordered list.
- Mark the current location with `aria-current="page"`.
- Keep separators decorative with `aria-hidden="true"`.
- Ensure the trail wraps cleanly on narrow screens.

Source: `components/breadcrumbs.html`.
