# Pagination

Semantic page navigation for multi-page content.

- Use a labeled `<nav>` landmark.
- Mark the active page with `aria-current="page"`.
- Give previous/next controls descriptive accessible labels.
- Replace the demo query-string URLs with application routes.

Source: `components/pagination.html`.
