const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const cards=document.querySelectorAll('.work article');if(!reduce)cards.forEach(card=>card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(700px) rotateX(${y*-3}deg) rotateY(${x*3}deg) translateY(-10px)`}));cards.forEach(card=>card.addEventListener('pointerleave',()=>card.style.transform=''));
