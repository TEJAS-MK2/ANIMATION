const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce){document.querySelectorAll('.feature-grid article').forEach(card=>card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.transform=`perspective(700px) rotateX(${-(e.clientY-r.top-r.height/2)/45}deg) rotateY(${(e.clientX-r.left-r.width/2)/45}deg) translateY(-7px)`}));document.querySelectorAll('.feature-grid article').forEach(card=>card.addEventListener('pointerleave',()=>card.style.transform=''));}
